services:
  ros2:
    build:
      context: ./docker
      dockerfile: Dockerfile.ros2-humble
    container_name: ros-mcp-test-ros2
    ports:
      - "9090:9090"
    healthcheck:
      test: ["CMD", "bash", "-c", "echo > /dev/tcp/localhost/9090"]
      interval: 2s
      timeout: 5s
      retries: 15
      start_period: 10s
